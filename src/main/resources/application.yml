spring:
  data:
    mongodb:
      uri: mongodb://admin:admin@localhost:27017
      database: transaction

management:
  endpoints:
    web:
      exposure:
        include: health, beans

transaction:
  validation:
    enabled: true
    banco: false
    horario: true